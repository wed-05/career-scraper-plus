thondef parse_job(provider):
    # Simulating job parsing based on provider
print(f"Parsing job data from {provider}...")
return {
"provider": provider,
"job_listings": [
{
"title": f"Software Engineer at {provider}",
"location": "Remote",
"id": "12345",
"url": f"https://{provider}.com/job/12345"
}
]
}